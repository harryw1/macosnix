\documentclass[11pt,letterpaper]{article}

% --- Metrics & Geometry ---
\usepackage[margin=1in,headheight=28pt]{geometry}
\usepackage{calc}

% --- Fix for Pandoc longtable counter ---
\makeatletter
\@ifundefined{c@none}{\newcounter{none}}{}
\makeatother

% --- Typography ---
\usepackage{fontspec}

\setmainfont{Palatino}
\setsansfont{Arial}
% Use JetBrains Mono Nerd Font if available, fall back to Courier New
\IfFontExistsTF{JetBrainsMono Nerd Font}{%
  \setmonofont{JetBrainsMono Nerd Font}[Scale=0.85]%
}{%
  \setmonofont{Courier New}[Scale=0.90]%
}

\usepackage{microtype}
\linespread{1.15}

% Prevent widows (single line at top of page) and orphans (single line at bottom)
\widowpenalty=10000
\clubpenalty=10000

% --- Colors ---
\usepackage{xcolor}
\definecolor{primary}{HTML}{203354}   % Deep Navy Blue
\definecolor{secondary}{HTML}{4A6572} % Muted Blue-Grey
\definecolor{accent}{HTML}{F9AA33}    % Gold
\definecolor{textgray}{HTML}{333333}
\definecolor{codebg}{gray}{0.96}
\definecolor{codeframe}{gray}{0.90}

% --- Links & Metadata ---
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=primary,
    filecolor=primary,
    urlcolor=primary,
    citecolor=primary,
    pdfborder={0 0 0},
    unicode=true,
    pdfencoding=auto,
    pdftitle={$if(title)$$title$$else$Report$endif$},
    pdfauthor={$if(author)$$author$$endif$}
}
\usepackage{xurl}
\usepackage{bookmark}   % Robust PDF bookmarks (load after hyperref)

% --- Paragraphs & Lists ---
\usepackage[parfill]{parskip}
\usepackage{enumitem}
\setlist{leftmargin=*, noitemsep, topsep=0.5em}

% --- Graphics & Captions ---
\usepackage{graphicx}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>\textheight\textheight\else\Gin@nat@height\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}

\usepackage{caption}
\captionsetup{
  font=small,
  labelfont=bf,
  format=plain,
  justification=justified,
  singlelinecheck=false
}
\usepackage{subcaption}  % Side-by-side figures: \begin{subfigure}[b]{0.48\textwidth}

% --- Tables ---
\usepackage{booktabs}         % \toprule \midrule \cmidrule \botrule
\usepackage{longtable}
\usepackage{array}
\usepackage{multirow}
\usepackage{wrapfig}
\usepackage{float}
\usepackage{colortbl}
\usepackage{pdflscape}        % \begin{landscape}...\end{landscape}
\usepackage{rotating}         % \begin{sidewaystable}...\end{sidewaystable}
\usepackage{threeparttable}
\usepackage{threeparttablex}
\usepackage[normalem]{ulem}
\usepackage{makecell}
\usepackage{xltabular}
\usepackage{etoolbox}
\usepackage{ragged2e}

\usepackage{siunitx}          % Decimal-aligned numeric columns: S[table-format=3.2]
\sisetup{
  group-separator={,},
  group-minimum-digits=4,
  detect-weight=true,
  detect-family=true
}

% --- Table Configuration ---
\setlength{\LTleft}{0pt plus 1fill}
\setlength{\LTright}{0pt plus 1fill}
\renewcommand{\arraystretch}{1.3}
\setlength{\tabcolsep}{8pt}

% Named fixed-width column types
\newcolumntype{L}[1]{>{\RaggedRight\arraybackslash}p{#1}}
\newcolumntype{C}[1]{>{\Centering\arraybackslash}p{#1}}
\newcolumntype{R}[1]{>{\RaggedLeft\arraybackslash}p{#1}}

% Full-width tabular* shorthand — fills \textwidth with evenly distributed spacing:
%   \begin{widetabular}{lcc...}
% Equivalent to: \begin{tabular*}{\textwidth}{@{\extracolsep\fill}lcc...}
\newenvironment{widetabular}[1]{%
  \begin{tabular*}{\textwidth}{@{\extracolsep\fill}#1}%
}{%
  \end{tabular*}%
}

% --- Advanced Line Breaking for Tables (XeLaTeX) ---
\newXeTeXintercharclass \BreakableChar
\newcount\charcount
\charcount=`\!
\loop\ifnum\charcount<`\~
  \XeTeXcharclass \charcount \BreakableChar
  \advance\charcount by 1
\repeat
\XeTeXinterchartoks \BreakableChar \BreakableChar = {\penalty 9999 \hskip 0pt plus 0.1pt\relax}

\AtBeginEnvironment{longtable}{%
  \XeTeXinterchartokenstate=1%
  \let\raggedright\RaggedRight%
}
\AtBeginEnvironment{tabular}{%
  \XeTeXinterchartokenstate=1%
  \let\raggedright\RaggedRight%
}
\AtBeginEnvironment{xltabular}{%
  \XeTeXinterchartokenstate=1%
  \let\raggedright\RaggedRight%
}

% --- Pandoc Code Highlighting ---
$if(highlighting-macros)$
$highlighting-macros$
$endif$

% --- GFM Extensions (Task Lists & Callouts) ---
\usepackage{amssymb}
\usepackage[most]{tcolorbox}

\newlist{checklist}{itemize}{2}
\setlist[checklist]{label=$$\square$$}
\newcommand{\cmark}{\checkmark}
\newcommand{\checkbox}{$$\square$$}
\newcommand{\donebox}{$$\boxtimes$$}

% Callout colors — visually distinct from primary/secondary heading palette
\definecolor{info-col}{HTML}{1565C0}    % Steel Blue  (distinct from primary navy)
\definecolor{tip-col}{HTML}{2E7D32}     % Forest Green
\definecolor{warn-col}{HTML}{F9AA33}    % Gold (accent)
\definecolor{danger-col}{HTML}{C62828}  % Deep Red

\newtcolorbox{callout}[2][]{
  enhanced,
  breakable,
  colback=#2!5!white,
  colframe=#2,
  coltitle=white,
  fonttitle=\bfseries\sffamily,
  title={#1},
  arc=2pt,
  boxrule=0.5pt,
  left=8pt, right=8pt, top=8pt, bottom=8pt,
  shadow={0.5mm}{-0.5mm}{0mm}{black!10}
}

\newenvironment{gfm-info}[1][Note]{\begin{callout}[#1]{info-col}}{\end{callout}}
\newenvironment{gfm-tip}[1][Tip]{\begin{callout}[#1]{tip-col}}{\end{callout}}
\newenvironment{gfm-warning}[1][Warning]{\begin{callout}[#1]{warn-col}}{\end{callout}}
\newenvironment{gfm-danger}[1][Important]{\begin{callout}[#1]{danger-col}}{\end{callout}}

% --- Headers & Footers ---
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[R]{\small\color{secondary}\sffamily \nouppercase{\leftmark}}
\fancyfoot[C]{\small\color{textgray}\thepage}
\renewcommand{\headrulewidth}{0.5pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{secondary}\leaders\hrule height \headrulewidth\hfill}}
\renewcommand{\footrulewidth}{0pt}

% --- Titles & Sections ---
\usepackage{titlesec}

\titleformat{\section}
  {\color{primary}\Large\bfseries\rmfamily}
  {\thesection}{1em}{}
  [\vspace{0.3em}{\color{secondary}\titlerule[1pt]}]

\titleformat{\subsection}
  {\color{secondary}\large\bfseries\sffamily}
  {\thesubsection}{1em}{}

\titleformat{\subsubsection}
  {\color{secondary}\normalsize\bfseries\sffamily}
  {\thesubsubsection}{1em}{}

\titlespacing*{\section}{0pt}{2em}{0.8em}
\titlespacing*{\subsection}{0pt}{1.2em}{0.5em}
\titlespacing*{\subsubsection}{0pt}{1em}{0.3em}

% --- Pandoc Tightlist Fix ---
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% --- Smart Cross-References ---
% \cref{fig:1} → "Figure 1", \cref{tab:a,eq:b} → "Table 1 and Equation 2"
% Must be loaded after hyperref and bookmark
\usepackage[capitalise,noabbrev]{cleveref}

% --- Custom Title Page ---
\makeatletter
\renewcommand{\maketitle}{
  \begin{titlepage}
    \vspace*{2cm}
    \noindent
    {\color{secondary}\rule{\linewidth}{2pt}}
    \par
    \vspace{2em}
    \noindent
    {\color{primary}\Huge \bfseries \sffamily $if(title)$$title$$else$Report Title$endif$} \\
    \vspace{0.5em}

    $if(subtitle)$
    \noindent
    {\color{secondary}\Large \sffamily $subtitle$} \\
    $endif$

    \vspace{2em}
    \noindent
    {\color{textgray}\large \today}

    \vfill

    \noindent
    \begin{minipage}{0.5\textwidth}
      \begin{flushleft} \large
        \textit{Prepared by:} \\
        \textbf{$if(author)$$author$$else$Harrison Weiss$endif$} \\
        \texttt{$if(email)$$email$$else$haweiss@dcas.nyc.gov$endif$}
      \end{flushleft}
    \end{minipage}%
    \begin{minipage}{0.5\textwidth}
      \begin{flushright} \large
        \textbf{$if(organization)$$organization$$else$DCAS BST$endif$} \\
      \end{flushright}
    \end{minipage}

    \vspace{2cm}
  \end{titlepage}
}
\makeatother

% --- Main Document ---
\title{$if(title)$$title$$else$Untitled Report$endif$}
\author{$if(author)$$author$$else$Harrison Weiss$endif$}
\date{$if(date)$$date$$else$\today$endif$}

\begin{document}

$if(title)$
\maketitle
$endif$

$if(abstract)$
\begin{abstract}
\noindent
$abstract$
\end{abstract}
\vspace{2em}
$endif$

$if(toc)$
{
\hypersetup{linkcolor=black}
\setcounter{tocdepth}{$if(toc-depth)$$toc-depth$$else$3$endif$}
\tableofcontents
\newpage
}
$endif$

$body$

\end{document}
